{
  "name": "websocket-doorlock",
  "version": "1.0.0",
  "description": "communication for send data from device",
  "main": "server.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "alim",
  "license": "ISC",
  "dependencies": {
    "babel-cli": "^6.26.0",
    "babel-preset-es2015": "^6.24.1",
    "socket.io": "^2.5.0"
  }
}
